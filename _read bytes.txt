// 1) 8 байт текста
// 2) флоат64
// 3) инт32

#pragma pack(push,1)
struct s_header
{
  char8 text[8];
  float64 n1;
  int32 n2;
};
#pragma pack(pop)

// читаем sizeof(s_header) в переменную типа s_header

s_header h;
read(&h,sizeof(h));

// оттуда узнаем, видимо, длину массива -> N

// затем читаем sizeof(float64) * N байтов

std::vector<float64> v(N,0);
read(&v[0],v.size());